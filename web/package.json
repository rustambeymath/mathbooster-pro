{
  "name": "mathbooster-pro-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && node scripts/sync-dist.mjs"
  },
  "devDependencies": {
    "acorn": "^8.14.1",
    "vite": "^5.4.0"
  }
}
